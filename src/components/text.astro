---

---

<p><slot /></p>

<style>
  p {
    font-weight: var(--fw-1);
    font-size: var(--fs-base);
    line-height: var(--lh-2);
    letter-spacing: var(--ls-1);
    text-wrap: balance;

    @media (width >= 48rem) {
      font-size: var(--fs-lg);
    }
  }
</style>
